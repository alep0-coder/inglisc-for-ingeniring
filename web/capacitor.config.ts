import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.inglese.engineering',
  appName: 'Fucking English',
  webDir: 'dist'
};

export default config;
